external class version {
    /** 主版本号（如 **2**.1.0 里的 **2**）*/
    var major: Number

    /** 次版本号（如 2.**1**.0 里的 **1**）*/
    var minor: Number

    /** 修订版本号（如 2.1.**0** 里的 **0**） */
    var revision: Number

    /** 当前版本是否为测试版 */
    var isBeta: Boolean
}