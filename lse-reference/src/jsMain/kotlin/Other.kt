import org.khronos.webgl.ArrayBuffer

//external class ByteBuffer : ArrayBuffer
typealias ByteBuffer = ArrayBuffer

// 防止使用
external val window: Nothing
external val document: Nothing
external val console: Nothing